import { useEffect, useState } from "react";
export function BroadcastCountdown({ target, label = "FEATURE BEGINS IN" }) { const [seconds, setSeconds] = useState(() => Math.max(0, Math.ceil((new Date(target).getTime() - Date.now()) / 1000))); useEffect(() => { const update = () => setSeconds(Math.max(0, Math.ceil((new Date(target).getTime() - Date.now()) / 1000))); update(); const id = window.setInterval(update, 250); return () => window.clearInterval(id); }, [target]); const minutes = Math.floor(seconds / 60); return <div className="wm-player-countdown"><span>{label}</span><strong>{String(minutes).padStart(2, "0")}:{String(seconds % 60).padStart(2, "0")}</strong></div>; }
